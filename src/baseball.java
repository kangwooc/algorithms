import java.util.Scanner;

public class baseball {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();

        for (int i = 0; i < n; i++) {
            int num = sc.nextInt();
            int ball = sc.nextInt();
            int strike = sc.nextInt();
        }
    }

    private static int numFinder(int num, int ball, int strike) {
        return 0;
    }
}
