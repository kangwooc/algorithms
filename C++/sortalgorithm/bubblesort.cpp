#include <iostream>
#include <algorithm>
using namespace std;
void bubbleSort(int*);

void bubbleSort(int* arr) {
    for (int i = 0; i < (sizeof(arr) / sizeof(int)) - 1; i++) {
        for (int j = 0; j < ; j++) {
            
        }
    }
}

int main(int argc, char const *argv[])
{
    /* code */
    int* arr = new int[100];
    for (int i = 0; i < 100; i++) {
        scanf("%d", arr[i]);
    }
    bubbleSort(arr);
    return 0;
}