#include <iostream>
#include <algorithm>
using namespace std;
void bubbleSort(int*);

void bubbleSort(int* arr) {
    for (int i = 0; i < arr)
}

int main(int argc, char const *argv[])
{
    /* code */
    int* arr = new int[100];
    for (int i = 0; i < 100; i++) {
        scanf("%d", arr[i]);
    }
    bubbleSort(arr);
    return 0;
}